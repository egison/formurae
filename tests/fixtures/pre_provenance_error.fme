mode collocated
dimension 1
axes x
discretization collocated centered accuracy 2
field u : scalar
def inner q = resample(q, 0)
def outer q = inner q
init:
  u := outer (x * x)
step:
  let held = outer u
  u' = held
