mode collocated
dimension 1
axes x

param dt = 0.1

macro half u =
  local q @ primal = ∂_x u
  in q

macro half u =
  local p @ primal = ∂_x u
  in p

field u : scalar @ primal

init:
  u = 0.0

step:
  u' = u
