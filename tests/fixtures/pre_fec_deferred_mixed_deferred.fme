mode collocated
dimension 2
axes x, y

field u : scalar
field w : scalar

init:
  u := sin (x) * cos (y)
  w := u

step:
  local v : tensor @ primal = grad u
  local energy : tensor = u * u
  w' = energy + withSymbols [i] (v~i . v_i)
  u' = u
