# Formurae の作業ルール

## 対象とするシミュレーション（最重要）

- **Formura 本体を拡張せず、既存の機能と通常のコード生成経路で実装できるシミュレーションに対象を絞る。** 例題・デモ・ベンチマークの新規追加と既存例の変更の両方に適用する。（ユーザー指示: 2026-09-10）
- Formurae は Formura のラッパーとして、`.fme -> Egison -> FEIR -> .fmr -> Formura -> C` の経路を使う。題材を成立させるために、Formura 本体へ機能を追加したり、Formura を経由せず C を生成したり、専用の実行基盤を追加したりしない。
- 題材の選定時に、初期条件・境界処理・時間積分・補助計算まで含めて、既存の `init`・`step`・`local` と Formura の機能で表現できることを確認する。近傍格子点の値を使う局所更新を中心とし、途中に全領域の圧力求解などを必要とする手法を無理に組み込まない。
- MPI による領域分割（複数の計算プロセスへの分担）と時間方向のブロッキング（複数ステップをまとめて計算する最適化）を活かせる題材を優先する。境界条件や更新式に対する既存実装の制約も選定時に確認する。
- 既存機能で表現できない処理が必要なら、不足と理由を報告し、対象の手法・題材を見直す。将来の Formura 拡張を扱う過去の文書は、デモ開発のために拡張へ着手する指示として扱わない。

## シミュレーション計算と描画の分担（最重要）

- **描画のための処理を除き、例題・デモ・ベンチマークのシミュレーションに必要な計算はすべて Formurae の `.fme` ソースに記述し、Formurae から生成したプログラムで実行する。** これは既存例の修正と新規追加の両方に適用する。（ユーザー指示: 2026-09-09）
- 対象には、支配方程式、初期条件、境界処理、時間積分とその中間段階、状態更新に必要な補間・座標変換・制約の適用・方程式の求解を含む。保存量など、数値的な検証のためにシミュレーションから求める物理量も Formurae 側で計算する。
- Python、NumPy、SciPy などにこれらの計算を委ねない。Python を手書きのモデル専用 C ソルバーへ置き換えるだけでも、この要件を満たさない。生成 C コードや Formurae 処理系内部の汎用演算の実装を実行することは、Formurae による実行に含む。
- **まず既存の構文・機能で表現する。** 時間積分の中間値には `local`、更新には `step`、境界処理には既存の境界指定や更新式を使えるか確認する。Formurae の数式記法や変換処理を改善する場合も、既存の Formura へ変換できる範囲に保ち、上記の対象範囲に従う。
- Python などで許される計算は、保存済みの結果から図・動画を作るための処理（表示用の補間、座標変換、色付け、描画など）に限る。描画用の計算結果をシミュレーションの初期条件・境界条件・次の状態へ戻さない。状態へ戻さないという理由だけで、描画以外の数値計算を許可しない。
- 外部の実行用スクリプトは、ビルド・起動、設定やファイルの受け渡し、結果の読み書き、描画などを担当してよいが、モデルの数値計算を担わない。
- 変更の確認では `.fme` だけでなく、呼び出すスクリプトや補助コードも確認し、描画以外のシミュレーション計算が外部に残っていないことを確認する。
